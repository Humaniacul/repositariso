// Optional shared build settings. Do not put OpenAI API keys here for app embedding — production uses Supabase Edge only.
// For local DEBUG direct OpenAI testing, set OPENAI_API_KEY in Xcode: Scheme → Run → Arguments → Environment Variables.
